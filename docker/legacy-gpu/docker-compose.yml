name: angevoice-legacy-gpu

services:
  angevoice-legacy-gpu:
    image: docker.io/maxblack777/angevoice-legacy-gpu:latest
    container_name: angevoice-legacy-gpu
    build:
      context: ../..
      dockerfile: docker/legacy-gpu/Dockerfile
    ports:
      # Host port:container port. Open http://HOST_IP:8102
      - "8102:8000"

    volumes:
      # Local model directory. Real model files require Git LFS or huggingface-cli download.
      - ../../models:/app/models:ro
      # Persist Hugging Face cache so downloaded model/voice files survive container recreation.
      - ../../.cache/huggingface:/root/.cache/huggingface
      # Development hot-reload. Uncomment for git pull + docker compose restart workflows.
      # - ../../src:/app/src:ro
      # Writable voices directory for optional admin voice upload.
      # - ../../models/voices:/app/models/voices:rw

    env_file:
      # Copy .env.example -> .env and customize, or use environment-specific files:
      #   cp .env.dev .env       (轻量开发)
      #   cp .env.staging .env   (预发布)
      #   cp .env.prod .env      (生产)
      - path: .env
        required: false
    environment:
      # Legacy GPU overrides: longer timeout, smaller segments
      - KOKORO_REQUEST_TIMEOUT_SECONDS=600
      - KOKORO_SEGMENT_LENGTH=80
      - KOKORO_STREAM_BINARY_ENABLED=false
      - KOKORO_CACHE_MAX_ITEMS=64
      - KOKORO_BATCH_MAX_ITEMS=10
      # GPU-specific: NVIDIA runtime settings
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility

    gpus: all

    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 6
      start_period: 150s

    restart: always
