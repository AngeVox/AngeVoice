# Optional local override for the first-entry admin password. The default deployment uses admin/admin123; for a private pre-hardened deployment, copy this file or generate it with:
#   python3 scripts/create_docker_admin_secret.py
# Never commit this file after filling a real value. Delete the password line after saving hashed credentials in /admin.
# ANGEVOICE_ADMIN_USERNAME=管理员
ANGEVOICE_ADMIN_PASSWORD=
